{
  "name": "animals-on-things-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Root package for Animals On Things project (Frontend + Firebase Functions)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "lint:functions": "npm run lint --prefix firebase/functions",
    "build:functions": "npm run build --prefix firebase/functions",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "npm run build --prefix frontend && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only database,storage",
    "deploy": "npm run deploy:rules && npm run deploy:functions && npm run deploy:hosting",
    "postinstall": "npm install --prefix frontend && npm install --prefix firebase/functions"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@types/uuid": "^10.0.0",
    "uuid": "^11.1.0"
  }
}
